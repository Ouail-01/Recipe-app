<!DOCTYPE html>
<html>
  <head>
    <title>Recipe App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar">
      <% if user_signed_in? %>
      <ul>
        <li><%= link_to 'Home', root_path %></li>
        <li><%= link_to 'Foods', foods_path %></li>
        <li><%= link_to 'Recipes list'%></li>
        <li><%= link_to 'Public recipe list', public_recipies_path %></li>
        <li><%= link_to 'Recip details'%></li>
        <li><%= link_to 'General shopping list'%></li>
        <li>
          <%= button_to "Log Out", destroy_user_session_path, method: :delete, data: { turbo: false } %>
        </li>
      </ul>
      <% else %>
        <ul class="flex justify-end gap-2 pr-6">
          <li><%= link_to "Sign up", new_user_registration_path %></li>
          <li><%= link_to "Sign in", new_user_session_path %> </li>
        </ul>
      <% end %>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
